import { Routes } from '@angular/router';
import { NcepRemoteComponent } from './ncep-remote/ncep-remote.component';

export const routes: Routes = [
  {
    path: '',
    component: NcepRemoteComponent,
  },
];
